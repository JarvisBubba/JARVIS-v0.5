JARVIS.v5.academics.learning=(()=>{const K='jarvis-v5-learning';const read=()=>{try{return JSON.parse(localStorage.getItem(K)||'{}')}catch(e){return {}}};const write=x=>localStorage.setItem(K,JSON.stringify(x));function record(topic,ok){const m=read();const r=m[topic]||{correct:0,total:0};r.total++;if(ok)r.correct++;m[topic]=r;write(m);return Math.round(100*r.correct/r.total)}function score(topic){const r=read()[topic];return r?Math.round(100*r.correct/r.total):0}return {record,score,all:read}})();
